import React, { useState } from "react";

const ReactGraph = () => {
  const [value, setValue] = useState(0);

  return <div className="child-container">ReactGraph</div>;
};

export default ReactGraph;
